{
  "name": "browsertrace-harness",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:release": "pnpm --filter @browsertrace/browsertrace build:release",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  }
}
